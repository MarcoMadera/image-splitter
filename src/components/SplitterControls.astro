---

---

<div class="relative z-10 mb-4 mt-4">
  <div
    class="flex flex-col items-center justify-center flex-wrap gap-2 sm:!flex-row sm:!items-end sm:justify-center sm:gap-4 xl:!justify-center"
  >
    <label class="dark:text-neutral-50">
      Grid X
      <input
        type="number"
        placeholder="Grid X"
        id="gridX"
        class="border border-gray-300 rounded-md p-2 text-black mb-1"
        value="3"
        min="1"
      />
    </label>
    <label class="dark:text-neutral-50">
      Grid Y
      <input
        type="number"
        placeholder="Grid Y"
        id="gridY"
        class="border border-gray-300 rounded-md p-2 text-black mb-1"
        value="3"
        min="1"
      />
    </label>
    <button
      type="button"
      id="downloadButton"
      disabled
      class="btn-regular tabbable px-4 py-3 cursor-pointer inline-flex items-center justify-center rounded-lg font-medium transition-[transform,background-color] duration-100 active:scale-105 md:px-6 opacity-100 disabled:!cursor-not-allowed disabled:!opacity-50"
      >Download
    </button>
  </div>
</div>
